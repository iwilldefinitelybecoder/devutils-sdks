# npm configuration for DevUtils SDKs monorepo

# Use exact versions
save-exact=true

# Require lockfile
require-lock=false

# Audit settings
audit-level=moderate

# Registry
registry=https://registry.npmjs.org/

# Workspace settings
workspaces-update=true
