# npm configuration for DevUtils SDKs monorepo

# Use exact versions
save-exact=true

# Require lockfile for production
require-lock=true

# Audit settings
audit-level=moderate

# Registry
registry=https://registry.npmjs.org/

# Workspace settings
workspaces-update=true

# Legacy peer deps for monorepo compatibility
legacy-peer-deps=true

